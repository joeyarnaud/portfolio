import React from "react"
import styled from "styled-components"

const SiteDisplay = styled.div`
  height: 30rem;
  width: 40rem;
`;

const SiteDisplayTitle = styled.h4`
  font-family: ${props => }
`

export default function SiteDisplay(props) {
  return <SiteDisplay>

  </SiteDisplay>;
}
